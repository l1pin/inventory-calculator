# Быстрый деплой на Netlify

## ✅ Что уже сделано

- [x] База данных в Supabase создана и настроена
- [x] Данные мигрированы в PostgreSQL
- [x] Netlify Functions созданы
- [x] Конфигурация готова

## 🚀 Деплой за 5 минут

### Шаг 1: Подготовка (30 сек)

Закоммитьте все изменения:

```bash
git add -A
git commit -m "Prepare for Netlify deployment"
git push origin main
```

### Шаг 2: Создание сайта в Netlify (2 минуты)

1. Перейдите на https://netlify.com
2. Войдите через GitHub
3. Нажмите **Add new site** → **Import an existing project**
4. Выберите **GitHub** → найдите ваш репозиторий `inventory-calculator`
5. Netlify автоматически определит настройки из `netlify.toml`
6. **НЕ нажимайте Deploy yet!** Сначала добавьте переменные окружения ⬇️

### Шаг 3: Добавление переменных окружения (1 минута)

⚠️ **ВАЖНО!** Добавьте эти переменные ПЕРЕД первым деплоем:

1. Внизу страницы нажмите **Add environment variables**
2. Добавьте:

```
SUPABASE_URL = https://mdrjojfgfdtkxrvhygky.supabase.co
SUPABASE_SERVICE_KEY = ваш_service_role_key_из_supabase
```

**Где взять Service Key:**
1. Supabase Dashboard → **Settings** → **API**
2. Скопируйте **service_role** key (⚠️ НЕ anon key!)

### Шаг 4: Деплой (2 минуты)

1. Нажмите **Deploy**
2. Дождитесь завершения (~2 минуты)
3. Получите URL типа `https://random-name.netlify.app`

✅ **Готово!** Ваш сайт онлайн!

---

## 🔍 Проверка

### 1. Откройте сайт

Кликните по URL в Netlify Dashboard

### 2. Проверьте API

Откройте: `https://your-site.netlify.app/.netlify/functions/health`

Должен вернуть:
```json
{
  "status": "OK",
  "database": "supabase",
  "platform": "netlify"
}
```

### 3. Проверьте загрузку данных

В интерфейсе:
- Таблицы должны загрузиться
- Данные должны отображаться
- Редактирование должно работать

---

## ❌ Проблемы?

### API не работает?

1. **Проверьте переменные:** Netlify Dashboard → Site settings → Environment variables
2. **Проверьте логи:** Netlify Dashboard → Functions → Logs
3. **Service Key правильный?** Должен начинаться с `eyJhbGci...`

### Данные не загружаются?

1. **Откройте Console (F12)** в браузере
2. Проверьте Network tab для ошибок
3. Убедитесь что в Supabase RLS выключен (DISABLE ROW LEVEL SECURITY)

### Сборка падает?

1. Проверьте логи: Netlify Dashboard → Deploys → (выберите деплой)
2. Убедитесь что все файлы закоммичены
3. Проверьте `package.json` на ошибки

---

## 🎨 Кастомный домен (опционально)

### Добавить свой домен:

1. Netlify Dashboard → **Domain management**
2. **Add custom domain** → введите ваш домен
3. Добавьте CNAME запись в DNS:
   - Type: `CNAME`
   - Name: `@` или `subdomain`
   - Value: `your-site.netlify.app`
4. SSL сертификат создастся автоматически (~1 мин)

---

## 🔄 Автоматический деплой

**Уже настроен!** При каждом `git push`:
1. Netlify автоматически обнаружит изменения
2. Соберет проект
3. Задеплоит новую версию

---

## 💰 Стоимость

### Бесплатно:
- ✅ Netlify: 100GB bandwidth, 300 build минут
- ✅ Supabase: 500MB БД, 2GB bandwidth
- ✅ SSL сертификат
- ✅ CDN
- ✅ Автодеплой

**Платить нужно только при превышении лимитов**

---

## 📚 Подробная документация

См. **DEPLOY.md** для:
- Детальных инструкций
- Troubleshooting
- Продвинутых настроек
- Мониторинга
- Оптимизации

---

## ✅ Чеклист после деплоя

- [ ] Сайт открывается
- [ ] API работает (проверьте /health endpoint)
- [ ] Данные загружаются
- [ ] Таблицы открываются
- [ ] Редактирование работает
- [ ] Удаление работает
- [ ] Категории работают

Все пункты отмечены? **Поздравляю! 🎉**

---

## 🆘 Нужна помощь?

1. Проверьте **DEPLOY.md** (раздел "Решение проблем")
2. Проверьте логи в Netlify Dashboard
3. Проверьте логи в Supabase Dashboard
4. Проверьте Browser Console (F12)

---

## Следующие шаги

После успешного деплоя:

1. ✅ Сделайте backup JSON файлов (на всякий случай):
   ```bash
   tar -czf data_backup_$(date +%Y%m%d).tar.gz data/
   ```

2. ✅ Настройте мониторинг:
   - Supabase Dashboard → Logs
   - Netlify Dashboard → Functions → Logs

3. ✅ Поделитесь ссылкой с командой!

Готово! 🚀
