# Быстрый старт миграции на Supabase

## Шаг 1: Создайте проект в Supabase (5 минут)

1. Перейдите на https://supabase.com и зарегистрируйтесь
2. Создайте новый проект:
   - **Project name:** `inventory-calculator`
   - **Database Password:** создайте надежный пароль и сохраните его
   - **Region:** выберите ближайший регион
3. Дождитесь создания проекта (~2 минуты)

## Шаг 2: Получите учетные данные

1. В Supabase Dashboard перейдите в **Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **Service Role Key** (⚠️ secret key, НЕ anon key!)

## Шаг 3: Настройте .env файл

```bash
# Скопируйте пример
cp .env.example .env

# Отредактируйте .env и вставьте ваши данные
nano .env
```

Замените значения:
```
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key_here
```

## Шаг 4: Установите зависимости

```bash
npm install @supabase/supabase-js dotenv
```

## Шаг 5: Выполните миграцию схемы БД

1. Откройте Supabase Dashboard → **SQL Editor**
2. Откройте файл `migrations/001_initial_schema.sql`
3. Скопируйте весь SQL код
4. Вставьте в SQL Editor и нажмите **Run**
5. Убедитесь, что все таблицы созданы (**Table Editor**)

## Шаг 6: Мигрируйте данные

```bash
node migrate-data.js
```

Это займет несколько минут в зависимости от объема данных.

## Шаг 7: Проверьте результат

1. Откройте Supabase Dashboard → **Table Editor**
2. Проверьте таблицы:
   - `tables` - должны быть ваши таблицы
   - `table_items` - товары
   - `crm_categories` - CRM категории
   - `item_categories` - категории товаров

## Шаг 8: Обновите серверный код (опционально)

См. подробную инструкцию в `MIGRATION_PLAN.md`

---

## Что дальше?

### Вариант A: Быстрый тест локально

Текущий сервер будет продолжать работать с JSON файлами.
Обновление кода для работы с БД - отдельная задача (см. MIGRATION_PLAN.md).

### Вариант B: Полная миграция

1. Обновить `server.js` для использования `db.js` вместо JSON
2. Протестировать локально
3. Развернуть на Netlify/собственный сервер (см. MIGRATION_PLAN.md)

---

## Откат в случае проблем

Все ваши JSON файлы остались без изменений!
Просто продолжайте использовать текущий сервер.

---

## Стоимость

### Бесплатный план Supabase включает:
- ✅ 500MB база данных
- ✅ 1GB файлового хранилища
- ✅ 2GB bandwidth
- ✅ 50,000 MAU (monthly active users)

Для вашего проекта этого должно хватить надолго.

---

## Поддержка

Если возникли вопросы или проблемы:
1. Проверьте `MIGRATION_PLAN.md` для подробной информации
2. Проверьте логи миграции в консоли
3. Проверьте Supabase Dashboard → **Logs** для ошибок БД

---

## Следующие шаги

После успешной миграции данных:

1. ✅ **Резервное копирование JSON** - создайте backup текущих JSON файлов
   ```bash
   cp -r data data_backup_$(date +%Y%m%d)
   ```

2. 📝 **Обновление кода** - обновите `server.js` для работы с БД
   - См. примеры в `db.js`
   - См. MIGRATION_PLAN.md Этап 5

3. 🧪 **Тестирование** - протестируйте все функции приложения

4. 🚀 **Деплой** - разверните на выбранной платформе
   - Netlify + Supabase (рекомендуется)
   - Собственный сервер
   - См. MIGRATION_PLAN.md Этап 6

---

## Файлы проекта

- `MIGRATION_PLAN.md` - подробный план миграции
- `migrations/001_initial_schema.sql` - SQL схема БД
- `migrate-data.js` - скрипт миграции данных
- `db.js` - клиент для работы с БД
- `.env.example` - пример конфигурации
- `QUICKSTART.md` - этот файл

Удачи! 🚀
