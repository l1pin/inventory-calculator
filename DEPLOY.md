# Деплой на Netlify с Supabase

## ✅ Что уже сделано

- [x] База данных создана в Supabase
- [x] Схема БД развернута
- [x] Данные мигрированы из JSON в PostgreSQL
- [x] Проверена работоспособность

## 🎯 Что будем делать

1. Создать Netlify Functions для API (serverless)
2. Настроить деплой frontend на Netlify
3. Подключить Supabase к Netlify
4. Задеплоить приложение

---

## Шаг 1: Создание Netlify аккаунта

1. Перейдите на https://netlify.com
2. Нажмите **Sign up** → выберите **GitHub** для входа
3. Авторизуйте Netlify в GitHub

✅ Готово!

---

## Шаг 2: Подготовка проекта

Все файлы уже подготовлены в вашем репозитории:
- `netlify.toml` - конфигурация Netlify
- `netlify/functions/` - serverless функции API
- Обновленный `package.json`

### Проверьте структуру:
```
inventory-calculator/
├── netlify.toml              # Конфигурация Netlify
├── netlify/
│   └── functions/            # Serverless API
│       ├── api.js           # Главный обработчик API
│       └── health.js        # Health check endpoint
├── client/                   # React приложение
├── db.js                    # Supabase клиент
└── package.json
```

---

## Шаг 3: Подключение репозитория к Netlify

### 3.1 Создание нового сайта

1. В Netlify Dashboard нажмите **Add new site** → **Import an existing project**
2. Выберите **Deploy with GitHub**
3. Авторизуйте доступ к вашему репозиторию `l1pin/inventory-calculator`
4. Выберите репозиторий из списка

### 3.2 Настройка сборки

Netlify автоматически определит настройки из `netlify.toml`, но проверьте:

**Site settings:**
- **Branch to deploy:** `main` (или ваша основная ветка)
- **Build command:** `npm run build:netlify`
- **Publish directory:** `client/build`
- **Functions directory:** `netlify/functions`

### 3.3 Настройка переменных окружения

⚠️ **ВАЖНО:** Нужно добавить переменные окружения!

1. В Netlify Dashboard → **Site settings** → **Environment variables**
2. Нажмите **Add a variable**
3. Добавьте следующие переменные:

| Key | Value | Где взять |
|-----|-------|-----------|
| `SUPABASE_URL` | `https://xxxxx.supabase.co` | Supabase Dashboard → Settings → API → Project URL |
| `SUPABASE_SERVICE_KEY` | `eyJhbGciOiJIUzI1...` | Supabase Dashboard → Settings → API → service_role (secret) |
| `NODE_VERSION` | `18` | Версия Node.js |

⚠️ **Используйте Service Role Key, НЕ anon key!**

### 3.4 Запуск деплоя

1. Нажмите **Deploy site**
2. Дождитесь завершения сборки (~2-5 минут)
3. Netlify автоматически назначит URL типа `https://random-name-123.netlify.app`

✅ Сайт задеплоен!

---

## Шаг 4: Проверка работоспособности

### 4.1 Откройте сайт

1. Перейдите по URL из Netlify Dashboard
2. Должен открыться ваш React интерфейс

### 4.2 Проверьте API

Откройте в браузере:
- `https://your-site.netlify.app/.netlify/functions/health` - должен вернуть статус OK

### 4.3 Проверьте загрузку данных

1. В интерфейсе проверьте, что таблицы загружаются
2. Попробуйте открыть таблицу
3. Попробуйте отредактировать товар

---

## Шаг 5: Настройка кастомного домена (опционально)

### 5.1 Добавление домена

1. В Netlify Dashboard → **Domain management**
2. Нажмите **Add custom domain**
3. Введите ваш домен (например, `inventory.example.com`)
4. Следуйте инструкциям для настройки DNS

### 5.2 Настройка DNS

Добавьте CNAME запись в вашем DNS провайдере:
```
Type: CNAME
Name: inventory (или @)
Value: your-site.netlify.app
```

### 5.3 SSL сертификат

Netlify автоматически выпустит SSL сертификат (Let's Encrypt) в течение ~1 минуты.

✅ Готово! Ваш сайт доступен по кастомному домену с HTTPS.

---

## Шаг 6: Автоматический деплой из Git

### Настроено автоматически!

После первого деплоя Netlify автоматически настроит:
- ✅ Деплой при каждом push в main ветку
- ✅ Preview для Pull Requests
- ✅ Rollback к предыдущим версиям

### Как это работает:

1. Вы делаете изменения в коде
2. Коммитите и пушите в GitHub:
   ```bash
   git add .
   git commit -m "Update feature"
   git push origin main
   ```
3. Netlify автоматически:
   - Обнаруживает изменения
   - Собирает проект
   - Деплоит новую версию

---

## Шаг 7: Мониторинг и логи

### Просмотр логов

1. Netlify Dashboard → **Deploys** → выберите деплой
2. Нажмите на билд для просмотра логов сборки

### Просмотр функций

1. Netlify Dashboard → **Functions**
2. Выберите функцию для просмотра логов выполнения

### Мониторинг в Supabase

1. Supabase Dashboard → **Logs**
2. Выберите **Database** или **API** для просмотра запросов

---

## Решение проблем

### Проблема: Сборка падает

**Решение:**
1. Проверьте логи в Netlify Dashboard → Deploys
2. Убедитесь, что все зависимости установлены
3. Проверьте `NODE_VERSION` в переменных окружения

### Проблема: API не работает

**Решение:**
1. Проверьте переменные окружения (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`)
2. Откройте Netlify Dashboard → Functions → Logs
3. Проверьте подключение к Supabase

### Проблема: 404 на страницах

**Решение:**
Убедитесь, что в `netlify.toml` есть редирект:
```toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### Проблема: Данные не загружаются

**Решение:**
1. Откройте браузерную консоль (F12)
2. Проверьте Network tab для ошибок API
3. Убедитесь, что Supabase доступен
4. Проверьте Row Level Security в Supabase (должен быть отключен)

---

## Альтернатива: Деплой через Netlify CLI

Если хотите деплоить из командной строки:

### Установка CLI

```bash
npm install -g netlify-cli
```

### Авторизация

```bash
netlify login
```

### Инициализация

```bash
netlify init
```

Следуйте инструкциям в терминале.

### Установка переменных

```bash
netlify env:set SUPABASE_URL "https://xxxxx.supabase.co"
netlify env:set SUPABASE_SERVICE_KEY "your_service_key"
```

### Деплой

```bash
# Тестовый деплой
netlify deploy

# Продакшн деплой
netlify deploy --prod
```

---

## Стоимость

### Netlify Free Tier:
- ✅ 100GB bandwidth/месяц
- ✅ 300 build minutes/месяц
- ✅ Unlimited сайтов
- ✅ Автоматический SSL
- ✅ Netlify Functions: 125,000 запросов/месяц

### Supabase Free Tier:
- ✅ 500MB база данных
- ✅ 2GB bandwidth
- ✅ 50,000 MAU

**Итого:** Полностью бесплатно для небольших и средних проектов!

---

## Следующие шаги

### После деплоя:

1. ✅ **Протестируйте все функции** - создание, редактирование, удаление таблиц
2. ✅ **Настройте мониторинг** - добавьте алерты в Supabase и Netlify
3. ✅ **Сделайте backup** - настройте автоматические бэкапы в Supabase (платная функция)
4. ✅ **Настройте CI/CD** - добавьте тесты перед деплоем
5. ✅ **Оптимизируйте** - включите кэширование, минификацию

### Дополнительно:

- Настройте **Supabase Auth** для многопользовательского доступа
- Включите **Row Level Security** в Supabase
- Добавьте **rate limiting** для API
- Настройте **CDN** для статических файлов

---

## Важные файлы

- `netlify.toml` - основная конфигурация
- `netlify/functions/` - API endpoints
- `db.js` - клиент Supabase
- `.env` - локальные переменные (НЕ коммитить!)
- `client/build/` - собранный frontend (генерируется автоматически)

---

## Поддержка

Если возникли проблемы:

1. Проверьте документацию:
   - Netlify: https://docs.netlify.com
   - Supabase: https://supabase.com/docs

2. Проверьте логи:
   - Netlify Dashboard → Functions → Logs
   - Supabase Dashboard → Logs

3. Проверьте настройки:
   - Переменные окружения
   - Конфигурацию `netlify.toml`
   - Row Level Security в Supabase (должен быть выключен)

---

## Итого

✅ База данных: Supabase (PostgreSQL)
✅ Backend API: Netlify Functions (serverless)
✅ Frontend: Netlify CDN (React)
✅ Домен: ваш-сайт.netlify.app (или кастомный)
✅ SSL: Автоматический (Let's Encrypt)
✅ Деплой: Автоматический из GitHub
✅ Стоимость: $0/месяц (на бесплатных планах)

Готово к работе! 🚀
