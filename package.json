{
  "name": "inventory-calculator-server",
  "version": "1.0.0",
  "description": "Сервер для калькулятора себестоимости с автосохранением",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run build:client",
    "build:client": "cd client && npm run build && cd .. && cp -r client/build .",
    "install:all": "npm install && cd client && npm install && cd ..",
    "deploy": "npm run install:all && npm run build:client && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": [
    "inventory",
    "calculator",
    "express",
    "react"
  ],
  "author": "Your Name",
  "license": "MIT"
}
