{
  "name": "inventory-calculator",
  "version": "2.0.0",
  "description": "Калькулятор себестоимости с Supabase и Netlify",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm run build:client",
    "build:client": "cd client && npm install && npm run build",
    "build:netlify": "npm run build:client",
    "install:all": "npm install && cd client && npm install && cd ..",
    "deploy": "npm run install:all && npm run build:client && npm start",
    "migrate": "node migrate-data.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "inventory",
    "calculator",
    "supabase",
    "netlify",
    "react"
  ],
  "author": "Your Name",
  "license": "MIT"
}
